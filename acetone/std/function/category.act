#acetone v0
#syntax verbose

identification division.
value-id. identity-function.

interface division.
linkage is external.
signature is
    for all a such that
        a -> a.

calculus division.
    I-combinator.

end-value.

identification division.
value-id. compose-functions.

interface division.
linkage is external.
signature is
    for all a b c such that
        (b -> c) ->
        (a -> b) ->
        (a -> c).

calculus division.
    over f g x abstract
        f (g x).

end-value.
